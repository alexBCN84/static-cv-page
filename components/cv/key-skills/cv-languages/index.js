import React from 'react';

const Languages = ({title, content}) => {
    return (
      <section>
        <h2>{title}</h2>
        <p>{content}</p>
      </section>
    )
}

export default Languages;